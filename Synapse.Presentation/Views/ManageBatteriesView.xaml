<UserControl x:Class="Synapse.Presentation.Views.ManageBatteriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <Style TargetType="Button" x:Key="ActionButtonStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="#CCCCCC" BorderThickness="1" Background="White" Padding="10">
            <StackPanel>
                <TextBlock Text="Batteries" FontWeight="Bold" Margin="0,0,0,10"/>
                <ListBox ItemsSource="{Binding Batteries}" SelectedItem="{Binding SelectedBattery}" DisplayMemberPath="Name" Height="300"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Add" Command="{Binding AddBatteryCommand}" Style="{StaticResource ActionButtonStyle}" Width="60"/>
                    <Button Content="Delete" Command="{Binding DeleteBatteryCommand}" Style="{StaticResource ActionButtonStyle}" Width="60"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" BorderBrush="#CCCCCC" BorderThickness="1" Background="White" Padding="10">
            <StackPanel>
                <TextBlock Text="Assign Sequence" FontWeight="Bold" Margin="0,0,0,10"/>
                <ComboBox ItemsSource="{Binding Sequences}" SelectedItem="{Binding SelectedSequence}" DisplayMemberPath="Name"/>
                <Button Content="Assign to Selected Battery" Command="{Binding AssignCommand}" Style="{StaticResource ActionButtonStyle}" Margin="0,10,0,0"/>
                <Button Content="Unassign" Command="{Binding UnassignCommand}" Style="{StaticResource ActionButtonStyle}" Margin="0,5,0,0"/>
                <TextBlock Text="Assignments" FontWeight="Bold" Margin="0,10,0,0"/>
                <ListBox ItemsSource="{Binding Assignments}" Height="150">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Battery "/>
                                <TextBlock Text="{Binding BatteryChannel}" FontWeight="Bold" Margin="5,0"/>
                                <TextBlock Text=": " />
                                <TextBlock Text="{Binding Sequence.Name}" Margin="5,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>